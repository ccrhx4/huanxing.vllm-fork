chunk_size: 256
remote_url: "mooncakestore://localhost:50001/"
remote_serde: "naive"
local_cpu: True
max_local_cpu_size: 30

extra_config:
  local_hostname: "localhost"
  metadata_server: "http://0.0.0.0:2379/metadata"
  protocol: "tcp"
  device_name: "" # Multiple RDMA devices can be specified as comma-separated list
  master_server_address: "localhost:50001"
  global_segment_size: 32212254720 # 30GB
  local_buffer_size: 1073741824 # 1GB
  transfer_timeout: 1
  save_chunk_meta: False
